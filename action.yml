name: Validate Bootstrap
description: Run validate-bootstrap.sh from agent-project-bootstrap (composite action for downstream repos)
branding:
  icon: check-circle
  color: green

runs:
  using: composite
  steps:
    - name: Validate bootstrap artifacts
      shell: bash
      run: bash "${{ github.action_path }}/scripts/validate-bootstrap.sh" --quick
